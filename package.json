{
  "name": "mixin-suede",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "npm run test --prefix ./environments/vite",
    "doc": "npm run generate:performance:documentation --prefix ./environments/vite && parkdown",
    "undoc": "parkdown -d"
  },
  "dependencies": {
    "typescript": "~5.9.3"
  },
  "devDependencies": {
    "@p-buddy/parkdown": "^0.0.34"
  }
}